//wip

//template custom_event script

//shouldPush decides whether the event should run the onPush function.
//return Globals.Function_Stop will prevent it from doing so.
function shouldPush(eventNote) {

}

//onPush is called once the song is loaded, so if you're using an event with a specific asset it should be loaded here.
function onPush(eventNote) {

}

//firstPush is called only on the first instance of this event being pushed.
function firstPush(eventNote) {

}

//onTrigger is called when the event is called.
//value1 and value2 are the arguments that were set in the chart editor.
function onTrigger(value1,value2) {

}